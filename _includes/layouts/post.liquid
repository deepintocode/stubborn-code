<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
        <base target="_blank"/>
        <link href="/favicon.png" rel="icon"/>
        <link href="/css/tailwind.css" rel="stylesheet"/>
        {% include _google_analytics.html %}
        <title>Stubborn Code -
            {{ title }}</title>
    </head>
    <body>
        {% include _header.html %}
        <div class="container px-6 sm:px-12 lg:px-48 pb-24 pt-12 m-auto">
            <div class="text-4xl text-brandDark">{{ title }}</div>
            <div class="flex text-gray-600">
                <span class="mx-1">{{ date | date: "%d %B, %Y" }}</span>
                <span>·</span>
                <span class="mx-1">{{ content | wordCount }}</span>
                {% if videoLink %}
                    <span>·</span>
                    <span class="mx-1">{{ videoDuration }}
                        to watch</span>
                {% endif %}
            </div>
            <div class="mt-8 text-3xl italic text-brandLight">
                {% if videoLink %}
                    <h3>tl;dw If you prefer a text version keep on reading!</h3>
                {% endif %}
            </div>
            <div class="markdown">
                {{ content }}
            </div>
        </div>
        {% include _footer.html %}
    </body>
</html>
